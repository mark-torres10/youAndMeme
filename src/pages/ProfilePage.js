import React from "react";

const ProfilePage = () => {
  return (
    <div className="profilePage" style={styles}>
      <h1>This is a profile page</h1>
    </div>
  );
};

const styles = {
  backgroundColor: "gray",
  width: "50%",
  border: "2px solid blue",
  margin: "auto",
};

export default ProfilePage;
